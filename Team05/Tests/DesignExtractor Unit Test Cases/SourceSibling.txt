Procedure SiblingTest {
  VarVar = b + c;
  ConsVons = 5 + 10;
  MathopMathop = a * b + c * d;
  VarCons = a + 5;
  ConsVar = 10 + b;
  ConsMathop = 5 + 3 * 4;
  MathopCons = 3 * 56 + 78;
  VarMathop = a + b * c;
  MathopVar = f * g + a;

  hard = a * 2 * b + a * b - 8 * b * b - a + 7 * a + 3 * b - a;
  extreme = 13 - (a + 3) * b * (a * (b + 2) * a - (a + b)) * 17 - (19 * (a + 11) * (b + 33)) + a + (b + 8 * a) * a * 7 - b;
}
