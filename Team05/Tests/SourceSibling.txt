Procedure SiblingTest {
  var-var = b + c;
  cons-cons = 5 + 10;
  mathop-mathop = a * b + c * d;
  var-cons = a + 5;
  cons-var = 10 + b;
  cons-mathop = 5 + 3 * 4;
  mathop-cons = 3 * 56 + 78;
  var-mathop = a + b * c;
  mathop-var = f * g + a;

  hard = a * 2 * b + a * b - 8 * b * b - a + 7 * a + 3 * b - a;
  extreme = 13 - (a + 3) * b * ( a * (b + 2) * a - (a + b)) * 17 - (19 * (a + 11) * (b + 33)) + a + (b + 8 * a) * a * 7 - b;
}